# Demo data storage

- `seed.json` is the committed sample data used to start the demo.
- `database.json` is generated automatically the first time the backend runs.
- `database.json` is ignored by Git so it never overwrites the shared seed data.

Use the **Reset demo data** button in the frontend to restore `seed.json` through the backend.
