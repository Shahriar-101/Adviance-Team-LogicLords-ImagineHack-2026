# Adviance React Frontend

This frontend is part of the full-stack Adviance MVP. Start the project from the repository root, not from this folder alone:

```bash
cd ..
npm install
cp backend/.env.example backend/.env
npm run dev
```

See the root `README.md` for the complete Gemini, backend, demo, and deployment instructions.
