{
  "name": "adviance-fullstack-mvp",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,magenta \"npm run dev -w adviance-backend\" \"npm run dev -w adviance-frontend\"",
    "build": "npm run build -w adviance-frontend",
    "start": "npm run start -w adviance-backend",
    "check": "npm run check -w adviance-backend && npm run build -w adviance-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
